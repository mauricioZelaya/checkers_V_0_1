image: java:8-jdk

before_script:
  - chmod +x gradlew
  - ./gradle --version

build-project:
  script:
    - ./gradlew build
    - ./gradlew allJar
  only:
    - develop, feature/CHK-013-GitLab-CI-CD

after_script:
  - echo "Checkers is up"