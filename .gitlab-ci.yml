image: java:8-jdk

before_script:
  - sdk install gradle 4.0.1
  - chmod +x gradlew

build_project:
  script:
    - ./gradlew build
    - ./gradlew allJar
  artifacts:
    expire_in: 1 week
  only:
    - develop, feature/CHK-013-GitLab-CI-CD

after_script:
  - echo "Checkers is up"